apiVersion: v1
kind: Service
metadata:
  name: <app-name>
  namespace: {{ .Values.environment }}
spec:
  externalTrafficPolicy: Cluster
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: <app-name>
  sessionAffinity: None
  type: NodePort