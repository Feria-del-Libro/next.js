apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name:  next-js
spec:
  minReplicas: {{ int .Values.hpa.replicaCount.min }}
  maxReplicas: {{ int .Values.hpa.replicaCount.max }}
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name:  next-js
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70