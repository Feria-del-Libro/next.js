apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: next-js
  name: next-js
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: next-js
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: next-js
    spec:
      {{- if eq .Values.environment "prod" }}
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - next-js
            topologyKey: "kubernetes.io/hostname"
      {{- end }}
      containers:
      - envFrom:
        - configMapRef:
            name: next-js-env
        image: {{ .Values.aws_account_id }}.dkr.ecr.us-east-1.amazonaws.com/rackoot/next-js:{{ .Values.app_version }}
        imagePullPolicy: Always
        name: next-js
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        lifecycle:
          postStart:
            exec:
              command:
                - "/bin/sh"
                - "-c"
                - "/usr/sbin/pushEnvVars.sh"
        resources:
          limits:
            cpu: 200m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /index.html
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /index.html
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        startupProbe:
          httpGet:
            path: /index.html
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
      nodeSelector:
        eks.amazonaws.com/nodegroup: {{ .Values.node_selector }}
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      serviceAccount: rackoot-iam
      serviceAccountName: rackoot-iam